# .github/workflows/calculator-ci.yml
# Pipeline de CI/CD para a Calculadora

name: Calculator CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  # Job de Testes Unitários
  unit-tests:
    name: Testes Unitários
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    
    steps:
    - name: Checkout código
      uses: actions/checkout@v3
    
    - name: Configurar Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}
    
    - name: Instalar dependências
      run: npm install
    
    - name: Executar testes unitários
      run: npm run test:unit
    
    - name: Gerar relatório de cobertura
      run: npm run test:coverage
    
    - name: Upload relatório de cobertura
      uses: codecov/codecov-action@v3
      with:
        files: ./coverage/coverage-final.json
        flags: unittests
        name: codecov-unit-tests

  # Job de Testes de Integração (Jest + JSDOM)
  integration-tests:
    name: Testes de Integração
    runs-on: ubuntu-latest
    needs: unit-tests
    
    steps:
    - name: Checkout código
      uses: actions/checkout@v3
    
    - name: Configurar Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18.x'
    
    - name: Instalar dependências
      run: npm ci
    
    - name: Executar testes de integração (Jest + JSDOM)
      run: npm run test:integration
    
    - name: Upload artefatos de testes
      if: failure()
      uses: actions/upload-artifact@v4
      with:
        name: integration-test-results
        path: |
          coverage/
          test-results/

  # Job de Análise de Código
  code-quality:
    name: Análise de Qualidade de Código
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout código
      uses: actions/checkout@v3
      with:
        fetch-depth: 0
    
    - name: Configurar Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18.x'
        cache: 'npm'
    
    - name: Instalar dependências
      run: npm ci
    
    - name: Executar ESLint
      run: npm run lint
      continue-on-error: true
    
    - name: Executar análise de segurança
      run: npm audit --audit-level=moderate
      continue-on-error: true
    
    - name: SonarCloud Scan
      uses: SonarSource/sonarcloud-github-action@master
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      continue-on-error: true

  # Job de Deploy (GitHub Pages)
  deploy:
    name: Deploy para GitHub Pages
    runs-on: ubuntu-latest
    needs: [unit-tests, integration-tests]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    permissions:
      contents: write
    
    steps:
    - name: Checkout código
      uses: actions/checkout@v3
    
    - name: Deploy para GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: .
        exclude_assets: '.github,node_modules,tests,coverage,.gitignore,package*.json,jest.config.js,*.md'
    
    - name: Notificação de deploy
      run: echo "✅ Deploy concluído com sucesso!"

  # Job de Relatório de Testes
  test-report:
    name: Relatório Consolidado de Testes
    runs-on: ubuntu-latest
    needs: [unit-tests, integration-tests]
    if: always()
    
    steps:
    - name: Gerar relatório consolidado
      run: |
        echo "## 📊 Relatório de Testes" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "✅ Testes Unitários: ${{ needs.unit-tests.result }}" >> $GITHUB_STEP_SUMMARY
        echo "✅ Testes de Integração & E2E (Jest+JSDOM): ${{ needs.integration-tests.result }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY